const version:string = "2.0.103";
const versions:string[] = [
    `V${version}`,
    `v${version}`,
    `V.${version}`,
    `v.${version}`
];
export const company:string = "13ninstudio";
export default versions;
